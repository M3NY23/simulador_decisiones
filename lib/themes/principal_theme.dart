import 'package:flutter/material.dart';

class PrincipalTheme {
  static getTheme() => ThemeData(
        useMaterial3: true,

        // Define the default brightness and colors.
        primaryColor: const Color(0xff002B7A)
        
      );
}
